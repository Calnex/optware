#!/bin/sh
mv downloads/ipkg-opt-0.99.163.tar.gz ../
sudo rm -rf builds/debian-root
rm -rf a.out downloads packages staging lib builds tmp host toolchain
mkdir downloads
mv ../ipkg-opt-0.99.163.tar.gz ./downloads/
USER=`whoami`
sudo chown -R $USER ./*
